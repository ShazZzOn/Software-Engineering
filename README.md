>Отчет по Теме #6 выполнил:
- Губанов Артём Юрьевич
- ИВТ-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |

## Лабораторная работа №6
### В школе, где вы учились, узнали, что вы крутой программист и попросили написать программу для учителей, которая будет при вводе кабинета писать для него ключ доступа и статус, занят кабинет или нет. При написании программы необходимо использовать словарь (dict), который на вход получает номер кабинета, а выводит необходимую информацию. Если кабинета, который вы ввели нет в словаре, то в консоль в виде значения ключа нужно вывести “None” и виде статуса вывести “False”. По большому счету написав данную программу мы с вами научились заменять иногда громоздкую конструкцию if/elif/else. Поскольку здесь функционал словаря полностью повторяет функционал условия, но при этом у использования словарей в более сложных программах есть намного больше возможностей реализации.
```python
request = int(input('Введите номер кабинета: '))
dictionary = {
    101: {'key': 1234, 'access': True},
    102: {'key': 1337, 'access': True},
    103: {'key': 8943, 'access': True},
    104: {'key': 5555, 'access': False},
    None: {'key': None, 'access': False},
}
response = dictionary.get(request)
if not response:
    response = dictionary[None]
key = response.get('key')
access = response.get('access')
print(key, access)
```
### Результат.
![Лаб 1](https://github.com/user-attachments/assets/ab5d43d2-fd58-4698-bdc9-7cbea34ce63f)

## Выводы
1. `request = int(input('Введите номер кабинета: '))`: запрашивает у пользователя номер кабинета и преобразует ввод в число.
2. `dictionary = {`: начинается создание словаря с данными кабинетов.
3. `101: {'key': 1234, 'access': True},`: кабинет 101 — код 1234, доступ разрешён.
4. `102: {'key': 1337, 'access': True},`: кабинет 102 — код 1337, доступ разрешён.
5. `103: {'key': 8943, 'access': True},`: кабинет 103 — код 8943, доступ разрешён.
6. `104: {'key': 5555, 'access': False},`: кабинет 104 — код 5555, доступ запрещён.
7. `None: {'key': None, 'access': False},`: шаблон для несуществующих кабинетов
8. `}`: конец словаря.
9. `response = dictionary.get(request)`: пытается получить данные по введённому номеру кабинета.
10. `if not response:`: проверяет, найден ли кабинет в словаре.
11. `response = dictionary[None]`: если кабинет не найден, подставляется шаблон None.
12. `key = response.get('key')`: извлекается значение кода доступа.
13. `access = response.get('access')`: извлекается флаг доступа True или False.
14. `print(key, access)`: выводит код и доступ к кабинету.

### Алексей решил создать самый большой словарь в мире. Для этого он придумал функцию dict_maker (**kwargs), которая принимает неограниченное количество параметров «ключ: значение» и обновляет созданный им словарь my_dict, состоящий всего из одного элемента «first» со значением «so easy». Помогите Алексею создать данную функцию.

```python
from pprint import pprint
my_dict = {'first': 'so easy'}
def dict_maker(**kwargs):
    my_dict.update(**kwargs)
dict_maker(a1=1, a2=20, a3=54, a4=13)
dict_maker(name='Михаил', age=31, weight=70, eyes_color='blue')
pprint(my_dict)
```
### Результат.
![лаб 2](https://github.com/user-attachments/assets/22410008-67cf-4fac-b895-18f907f6c3b4)

## Выводы
1. `from pprint import pprint`: импортируется функция pprint, которая красиво форматирует вывод словарей.
2. `my_dict = {'first': 'so easy'}`: создаётся словарь my_dict с одним элементом — ключ 'first' и значение 'so easy'.
3. `def dict_maker(**kwargs):`: определяется функция dict_maker, принимающая произвольное количество именованных аргументов.
4. `my_dict.update(**kwargs)`: обновляет словарь my_dict, добавляя или изменяя пары ключ–значение, переданные в функцию.
5. `dict_maker(a1=1, a2=20, a3=54, a4=13)`: добавляет в словарь ключи 'a1', 'a2', 'a3', 'a4' с соответствующими значениями.
6. `dict_maker(name='Михаил', age=31, weight=70, eyes_color='blue')`: добавляет новые пары 'name', 'age', 'weight', 'eyes_color' в словарь.
7. `pprint(my_dict)`: красиво выводит обновлённый словарь на экран.

### Для решения некоторых задач бывает необходимо разложить строку на отдельные символы. Мы знаем что это можно сделать при помощи split(), у которого более гибкая настройка для разделения для этого, но если нам нужно посимвольно разделить строку без всяких условий, то для этого мы можем использовать кортежи (tuple).

```python
input_string = 'HelloWorld'
result = tuple(input_string)
print(result)
print(list(result))
```
### Результат.
<img width="2559" height="1599" alt="image" src="https://github.com/user-attachments/assets/a1875a41-1d81-410a-acf4-abed96d02457" />

## Выводы
1. `input_string = 'HelloWorld'`: создаётся строка input_string со значением 'HelloWorld'.
2. `result = tuple(input_string)`: преобразует строку в кортеж, где каждый символ строки становится отдельным элементом.
3. `print(result)`: выводит полученный кортеж на экран.
4. `print(list(result))`: преобразует кортеж в список и выводит его.

### Вовочка решил написать крутую функцию, которая будет писать имя, возраст и место работы, но при этом на вход этой функции будет поступать кортеж. Помогите Вовочке написать эту программу.

```python
def personal_info(name, age, company='unnamed'):
    print(f"Имя: {name} Возраст: {age} Компания: {company}")
tom = ("Григорий", 22)
personal_info(*tom)
bob = ("Георгий", 41, "Yandex")
personal_info(*bob)
```
### Результат.
![лаб 4](https://github.com/user-attachments/assets/c59d520d-8349-4f7f-954f-76c611060352)

## Выводы
1. `def personal_info(name, age, company='unnamed'):`: функция с параметрами: имя, возраст, компания (по умолчанию).
2. `print(f"Имя: {name} Возраст: {age} Компания: {company}")`: выводит данные в удобном формате.
3. `tom = ("Григорий", 22)`: создаётся кортеж с именем и возрастом.
4. `personal_info(*tom)`: кортеж распаковывается: передаются name='Григорий', age=22, а company берётся по умолчанию.
5. `bob = ("Георгий", 41, "Yandex")`: кортеж с тремя значениями.
6. `personal_info(*bob)`: распаковывает все значения, включая название компании.

### Для сопровождения первых лиц государства X нужен кортеж, но никто не может определиться с порядком машин, поэтому вам нужно написать функцию, которая будет сортировать кортеж, состоящий из целых чисел по возрастанию, и возвращает его. Если хотя бы один элемент не является целым числом, то функция возвращает исходный кортеж.

```python
def tuple_sort(tpl):
    for elm in tpl:
        if not isinstance(elm, int):
            return tpl
    return tuple(sorted(tpl))
if __name__== '__main__':
    print(tuple_sort((5, 5, 3, 1, 9)))
    print(tuple_sort((5, 5, 2.1, '1', 9)))
```
### Результат.
![лаб 5](https://github.com/user-attachments/assets/e817f316-579e-4839-8aa7-d9ad1f2a9d34)

## Выводы
1. `def tuple_sort(tpl):`: создаётся функция, принимающая кортеж tpl.
2. `for elm in tpl:`: перебираются все элементы кортежа.
3. `if not isinstance(elm, int):`: проверяется, что элемент не является целым числом.
4. `return tpl`: если найден нецелый элемент, функция возвращает исходный кортеж без изменений.
5. `return tuple(sorted(tpl))`: если все элементы целые, возвращается новый отсортированный кортеж.
6. `if __name__== '__main__':`: проверка, что код запущен напрямую, а не импортирован.
7. `print(tuple_sort((5, 5, 3, 1, 9)))`: все элементы целые, выводит отсортированный кортеж (1, 3, 5, 5, 9).
8. `print(tuple_sort((5, 5, 2.1, '1', 9)))`: есть нецелые элементы, возвращает исходный кортеж (5, 5, 2.1, '1', 9).

## Самостоятельная работа №6
### При создании сайта у вас возникла потребность обрабатывать данные пользователя в странной форме, а потом переводить их в нужные вам форматы. Вы хотите принимать от пользователя последовательность чисел, разделенных пробелом, а после переформатировать эти данные в список и кортеж. Реализуйте вашу задумку. Для получения начальных данных используйте input(). Результатом программы будет выведенный список и кортеж из начальных данных.

```python
data = input("Введите числа через пробел: ")
numbers = data.split()
numbers_list = [int(i) for i in numbers]
numbers_tuple = tuple(numbers_list)
print(numbers_list)
print(numbers_tuple)
```
### Результат.
![Сам 1](https://github.com/user-attachments/assets/306b7009-4f4d-45a4-a2c1-6ec631c03747)

## Выводы
1. `data = input("Введите числа через пробел: ")`: пользователь вводит числа через пробел, сохраняются как строка.
2. `numbers = data.split()`: строка разбивается на список подстрок по пробелам.
3. `numbers_list = [int(i) for i in numbers]`: каждая подстрока преобразуется в целое число, создаётся список numbers_list.
4. `numbers_tuple = tuple(numbers_list)`: список numbers_list преобразуется в кортеж numbers_tuple.
5. `print(numbers_list)`: выводится список чисел.
6. `print(numbers_tuple)`: выводится кортеж чисел.

### Николай знает, что кортежи являются неизменяемыми, но он очень упрямый и всегда хочет доказать, что он прав. Студент решил создать функцию, которая будет удалять первое появление определенного элемента из кортежа по значению и возвращать кортеж без него. Попробуйте повторить шедевр не признающего авторитеты начинающего программиста. Но учтите, что Николай не всегда уверен в наличии элемента в кортеже (в этом случае кортеж вернется функцией в исходном виде). Входные данные: (1, 2, 3), 1) (1, 2, 3, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2), 3) (2, 4, 6, 6, 4, 2), 9) Ожидаемый результат: (2, 3) (1, 2, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2) (2, 4, 6, 6, 4, 2)

```python
def remove_first(tpl, value):
    if value in tpl:
        lst = list(tpl)
        lst.remove(value)
        return tuple(lst)
    return tpl
print(remove_first((1, 2, 3), 1))
print(remove_first((1, 2, 3, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2), 3))
print(remove_first((2, 4, 6, 6, 4, 2), 9))
```
### Результат.
![Сам 2](https://github.com/user-attachments/assets/cd676f04-c0a7-4020-a47b-436f8c8c50a8)

## Выводы
1. `def remove_first(tpl, value):`: создаётся функция, которая принимает кортеж tpl и значение value для удаления.
2. `if value in tpl:`: проверяется, содержится ли value в кортеже.
3. `lst = list(tpl)`: кортеж преобразуется в список lst, чтобы можно было изменить элементы.
4. `lst.remove(value)`: удаляется первое вхождение значения value из списка.
5. `return tuple(lst)`: список преобразуется обратно в кортеж и возвращается.
6. `return tpl`: если value не найдено, возвращается исходный кортеж.
7. `print(remove_first((1, 2, 3), 1))`: удаляется 1, вывод: (2, 3).
8. `print(remove_first((1, 2, 3, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2), 3))`: удаляется первое 3 вывод (1, 2, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2).
9. `print(remove_first((2, 4, 6, 6, 4, 2), 9))`: 9 нет в кортеже вывод (2, 4, 6, 6, 4, 2).

### ) Ребята поспорили кто из них одним нажатием на numpad наберет больше повторяющихся цифр, но не понимают, как узнать победителя. Вам им нужно в этом помочь. Дана строка в виде случайной последовательности чисел от 0 до 9 (длина строки минимум 15 символов). Требуется создать словарь, который в качестве ключей будет принимать данные числа (т. е. ключи будут типом int), а в качестве значений – количество этих чисел в имеющейся последовательности. Для построения словаря создайте функцию, принимающую строку из цифр. Функция должна возвратить словарь из 3-х самых часто встречаемых чисел, также эти значения нужно вывести в порядке возрастания ключа.

```python
from collections import Counter
def top_three_digits(s):
    counts = Counter(int(ch) for ch in s)
    most_common = counts.most_common(3)
    top_dict = {k: v for k, v in sorted(most_common)}
    return top_dict
random_digits = "1123456677889901234"
print(top_three_digits(random_digits))
```
### Результат.
![Сам 3](https://github.com/user-attachments/assets/7f78a54e-834f-4c57-a59d-387f2ae44624)

## Выводы
1. `from collections import Counter`: импортируется класс Counter для подсчёта частоты элементов.
2. `def top_three_digits(s):`: создаётся функция, принимающая строку s.
3. `counts = Counter(int(ch) for ch in s)`: преобразует каждый символ строки в число и подсчитывает, сколько раз каждая цифра встречается.
4. `most_common = counts.most_common(3)`: берёт три наиболее часто встречающиеся цифры и их количества.
5. `top_dict = {k: v for k, v in sorted(most_common)}`: создаёт словарь из трёх цифр, сортируя их по ключу (цифре).
6. `return top_dict`: возвращает словарь с тремя самыми частыми цифрами.
7. `random_digits = "1123456677889901234"`: строка случайных цифр.
8. `print(top_three_digits(random_digits))`: выводит словарь трёх самых частых цифр.

### Ваш хороший друг владеет офисом со входом по электронным картам, ему нужно чтобы вы написали программу, которая показывала в каком порядке сотрудники входили и выходили из офиса. Определение сотрудника происходит по id. Напишите функцию, которая на вход принимает кортеж и случайный элемент (id), его можно придумать самостоятельно. Требуется вернуть новый кортеж, начинающийся с первого появления элемента в нем и заканчивающийся вторым его появлением включительно. Если элемента нет вовсе – вернуть пустой кортеж. Если элемент встречается только один раз, то вернуть кортеж, который начинается с него и идет до конца исходного. Входные данные: (1, 2, 3), 8) (1, 8, 3, 4, 8, 8, 9, 2), 8) (1, 2, 8, 5, 1, 2, 9), 8) Ожидаемый результат: () (8, 3, 4, 8) (8, 5, 1, 2, 9)

```python
def slice_between(tpl, elem):
    if elem not in tpl:
        return ()
    first = tpl.index(elem)
    try:
        second = tpl.index(elem, first + 1) + 1
        return tpl[first:second]
    except ValueError:
        return tpl[first:]
print(slice_between((1, 2, 3), 8))
print(slice_between((1, 8, 3, 4, 8, 8, 9, 2), 8))
print(slice_between((1, 2, 8, 5, 1, 2, 9), 8))
```
### Результат.

## Выводы
1. `def slice_between(tpl, elem):`: создаётся функция, которая принимает кортеж tpl и элемент elem.
2. `if elem not in tpl:`: проверяет, содержится ли элемент в кортеже.
3. `return ()`: если элемента нет, возвращается пустой кортеж.
4. `first = tpl.index(elem)`: находит индекс первого вхождения элемента.
5. `try:`: пробует найти второе вхождение элемента.
6. `second = tpl.index(elem, first + 1) + 1`: ищет индекс второго вхождения после первого и добавляет 1, чтобы включить второй элемент в срез
7. `return tpl[first:second]`: возвращает срез от первого до второго вхождения элемента включительно.
8. `except ValueError:`: если второго вхождения нет, возникает ошибка ValueError.
9. `return tpl[first:]`: возвращает срез от первого вхождения элемента до конца кортежа.
10. `print(slice_between((1, 2, 3), 8))`: элемента 8 нет вывод: ()
11. `print(slice_between((1, 8, 3, 4, 8, 8, 9, 2), 8))`: берётся срез от первого до второго 8 включительно (8, 3, 4, 8)
12. `print(slice_between((1, 2, 8, 5, 1, 2, 9), 8))`: только одно вхождение (8, 5, 1, 2, 9)

### Самостоятельно придумайте и решите задачу, в которой будут обязательно использоваться кортеж или список. Проведите минимум три теста для проверки работоспособности вашей задачи.

```python
def increase_by_smaller_sum(lst):
    result = []
    for x in lst:
        smaller_sum = sum(y for y in lst if y < x)
        result.append(x + smaller_sum)
    return result
test1 = [1, 2, 3]
test2 = [5, 1, 4, 2]
test3 = [10, 5, 15, 3]
print(increase_by_smaller_sum(test1))  
print(increase_by_smaller_sum(test2))  
print(increase_by_smaller_sum(test3))  
```
### Результат.
![Сам 5](https://github.com/user-attachments/assets/b8730008-fb78-4c0b-92cd-53f32e33b9a5)

## Выводы
1. `def increase_by_smaller_sum(lst):`: создаётся функция, принимающая список lst.
2. `result = []`: создаётся пустой список для хранения результатов.
3. `for x in lst:`: перебираются все элементы списка.
4. `smaller_sum = sum(y for y in lst if y < x)`: вычисляется сумма всех элементов списка, которые меньше текущего x.
5. `result.append(x + smaller_sum)`: к текущему элементу x прибавляется сумма меньших элементов и результат добавляется в список result.
6. `return result`: функция возвращает новый список с увеличенными элементами.
7. `test1 = [1, 2, 3]`: первый тестовый список.
8. `test2 = [5, 1, 4, 2]`: второй тестовый список.
9. `test3 = [10, 5, 15, 3]`: третий тестовый список.
10. `print(increase_by_smaller_sum(test1))`: вывод результата для первого списка [1, 3, 6]
11. `print(increase_by_smaller_sum(test2))`: вывод результата для второго списка [12, 1, 10, 3]
12. `print(increase_by_smaller_sum(test3))`: вывод результата для третьего списка [15, 3, 30, 0]
