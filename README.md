>Отчет по Теме #9 выполнил:
- Губанов Артём Юрьевич
- ИВТ-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + |   |
| Задание 3 | + |   |
| Задание 4 | + |   |
| Задание 5 | + |   |

## Лабораторная работа №9
### 
```python

```
### Результат.

## Выводы
1. ``: объявляем класс, описывающий «человека по имени Иван»
2. ``: ограничиваем допустимые атрибуты экземпляра только кортежом ('name',)
3. ``: конструктор; запускается при создании объекта
4. ``: сохраняем переданное имя в разрешённый слот 'name'
5. ``: сравниваем имя с эталоном
6. ``: если совпало — выводим подтверждение
7. ``: иначе — имя не угадали
8. ``: печатаем сообщение c конкретным именем
9. ``: создаём первый объект; в __init__ выведется фраза «не угадали»
10. ``: создаём второй объект; в __init__ выведется «Да, я Иван»
11. ``: проверка доступа к допустимому атрибуту 'name'
12. ``: попытка добавить запрещённый атрибут
13. ``: вызовет AttributeError из-за __slots__
    
### 
```python

```
### Результат.

## Выводы
1. ``: описываем «мороженое»
2. ``: конструктор; по умолчанию добавки нет
3. ``: если передан аргумент и он строка —
4. ``: сохраняем его; иначе пишем None
5. ``: метод, печатающий состав
6. ``: если топпинг задан (строка не пустая)
7. ``: выводим «Мороженое с ...»
8. ``: иначе
9. ``: выводим «Обычное мороженое»
10. ``: экземпляр без аргументов → топпинга нет
11. ``: печать состава (должно быть «Обычное мороженое»)
12. ``: экземпляр с корректной строкой
13. ``: печать состава (должно быть «с шоколадом»)
14. ``: передаём число (не строка)
15. ``: снова «Обычное мороженое»

### 
```python

```
### Результат.

## Выводы
1. ``: класс-демо для инкапсуляции
2. ``: конструктор принимает стартовое значение
3. ``: «приватный по соглашению» атрибут
4. ``: setter — задаёт новое значение
5. ``: записываем в «приватный» атрибут
6. ``: getter — возвращает текущее значение
7. ``: отдаём содержимое
8. ``: deleter — удаляет атрибут
9. ``: оператор del снимает атрибут у объекта
10. ``: создаём объект-свойство с тремя методами
11. ``: функция чтения
12. ``: функция записи
13. ``: функция удаления
14. ``: строка-докстринг для свойства
15. ``: создаём экземпляр с _value=42
16. ``: 42 — читаем через getter
17. ``: меняем значение на 45
18. ``: 45 — проверяем
19. ``: ещё раз меняем
20. ``: удаляем атрибут _value целиком
21. ``: вызовет AttributeError: _value удалён

### 
```python

```
### Результат.

## Выводы
1. ``: базовый класс «Млекопитающее»
2. ``: общая характеристика для всех потомков
3. ``: класс «Собака» наследует Mammal
4. ``: свой атрибут — вид
5. ``: свой атрибут — «говорит»
6. ``: класс «Кошка» наследует Mammal
7. ``: свой атрибут — вид
8. ``: свой атрибут — «говорит»
9. ``: экземпляр собаки
10. ``: унаследованный className + свой звук
11. ``: экземпляр кошки
12. ``: аналогично для кошки

### 
```python
class Mammal:                               
    className = 'Mammal'                    
class Dog(Mammal):                          
    species = 'canine'                      
    sounds = 'wow'                          
class Cat(Mammal):                          
    species = 'feline'                      
    sounds = 'meow'                         
dog = Dog()                                 
print(f"Dog is {dog.className}, but they say {dog.sounds}")   
cat = Cat()                                 
print(f"Cat is {cat.className}, but they say {cat.sounds}") 
```
### Результат.

## Выводы
1. ``: базовый класс «Млекопитающее»
2. ``: общая характеристика для всех потомков
3. ``: класс «Собака» наследует Mammal
4. ``: свой атрибут — вид
5. ``: свой атрибут — «говорит»
6. ``: класс «Кошка» наследует Mammal
7. ``: свой атрибут — вид
8. ``: свой атрибут — «говорит»
9. ``: экземпляр собаки
10. ``: унаследованный className + свой звук
11. ``: экземпляр кошки
12. ``: аналогично для кошки

### 
```python
class Russian:                              
    @staticmethod                           
    def greeting():                         
        print("Привет")                     
class English:                              
    @staticmethod                           
    def greeting():                         
        print("Hello")                      
def greet(language):                        
    language.greeting()                     
ivan = Russian()                            
greet(ivan)                                 
john = English()                            
greet(john)                                 
```
### Результат.

## Выводы
1. ``: класс для «русского языка»
2. ``: метод, независимый от экземпляра 
3. ``: интерфейс «как здороваемся»
4. ``: конкретная реализация
5. ``: класс для «английского языка»
6. ``: снова статический метод
7. ``: та же «форма» метода
8. ``: но другая реализация
9. ``: полиморфная функция — принимает любой «язык»
10. ``: вызывает одноимённый метод, не заботясь о типе
11. ``: объект «русский»
12. ``: печатает «Привет»
13. ``: объект «английский»
14. ``: печатает «Hello»

### 
```python
class Tomato:                                             
    """Один помидор: индекс и стадия созревания."""       
    states = ('отсутствует', 'цветение', 'зелёный', 'красный')  
    def __init__(self, index: int):                       
        self._index = index                               
        self._state = Tomato.states[0]                    
    def grow(self):                                       
        i = Tomato.states.index(self._state)              
        if i < len(Tomato.states) - 1:                    
            self._state = Tomato.states[i + 1]            
    def is_ripe(self) -> bool:                            
        return self._state == Tomato.states[-1]           
    def __repr__(self):                                   
        return f"Tomato({self._index}, state='{self._state}')"   
class TomatoBush:                                          
    def __init__(self, count: int):                       
        self.tomatoes = [Tomato(i + 1) for i in range(count)]   
    def grow_all(self):                                   
        for t in self.tomatoes:                           
            t.grow()                                      
    def all_are_ripe(self) -> bool:                       
        return all(t.is_ripe() for t in self.tomatoes)    
    def give_away_all(self):                              
        self.tomatoes.clear()                             
class Gardener:                                           
    @staticmethod                                        
    def knowledge_base():                                 
        print("Справка: поливайте и ухаживайте — куст перейдёт через стадии:",", ".join(Tomato.states))                   
    def __init__(self, name: str, plant: TomatoBush):     
        self.name = name                                  
        self._plant = plant                               
    def work(self):                                       
        print(f"{self.name} ухаживает за кустом…")        
        self._plant.grow_all()                            
    def harvest(self):                                    
        if self._plant.all_are_ripe():                    
            print(f"{self.name} собирает урожай!")        
            self._plant.give_away_all()                   
        else:                                             
            print("Предупреждение: есть неспелые плоды — рано собирать.")  
    def __repr__(self):                                   
        return f"Gardener({self.name})"                   
Gardener.knowledge_base()                                  
bush = TomatoBush(3)                                       
g = Gardener("Анна", bush)                                 
g.work()                                                   
print(bush.tomatoes)                                      
g.harvest()                                               
g.work()                                                  
print(bush.tomatoes)                                      
g.harvest()                                               
g.work()                                                  
print(bush.tomatoes)                                      
g.harvest()                                               
print("Состояние после сбора:", bush.tomatoes)                                      
```
### Результат.

## Выводы
1. ``: описывает один помидор
2. ``: докстринг для читаемости
3. ``: статическое свойство со всеми стадиями
4. ``: конструктор принимает порядковый номер плода
5. ``: «приватный» индекс плода 
6. ``: начальная стадия — первая из списка states
7. ``: переводит плод на следующую стадию
8. ``: находим текущую позицию стадии в кортеже
9. ``: если это не последняя стадия
10. ``: двигаем состояние на один шаг вперёд
11. ``: проверяет, дозрел ли помидор
12. ``: True, если стадия — последняя 
13. ``: «служебное» представление для печати списков
14. ``: удобная строка со стадией
15. ``: куст содержит несколько помидоров
16. ``: конструктор принимает количество плодов
17. ``: создаём список объектов Tomato 
18. ``: все помидоры растут на шаг
19. ``: перебираем каждый плод
20. ``: переводим его на следующую стадию
21. ``: True, если все плоды дозрели
22. ``: проверяем методом is_ripe у каждого плода
23. ``: «сбор урожая»: очищаем список плодов
24. ``: удаляем все элементы списка на кусте
25. ``: садовник ухаживает за растением
26. ``: справка — статический метод, не требует self
27. ``: выводит базовые советы
28. ``: соединяем стадии в строку
29. ``: конструктор принимает имя и объект куста
30. ``: публичное динамическое свойство — имя садовника
31. ``: «приватное» динамическое свойство — ссылка на куст
32. ``: работа садовника: ухаживать за растением
33. ``: информируем о действии
34. ``: просим куст «расти» — все плоды переходят на шаг
35. ``: попытка собрать урожай
36. ``: если все плоды дозрели
37. ``: сообщение об успешном сборе
38. ``: очищаем список плодов на кусте
39. ``: иначе
40. ``: предупреждаем пользователя
41. ``: наглядное представление объекта Gardener
42. ``: возвращаем строку с именем
43. ``: выводим справку по садоводству
44. ``: создаём куст с тремя помидорами
45. ``: создаём садовника Анну, привязываем куст
46. ``: первый цикл ухода: все плоды переходят на «цветение»
47. ``: печать текущих состояний плодов
48. ``: пробуем собрать — ещё рано
49. ``: второй цикл ухода: «зелёный»
50. ``: печать текущих состояний
51. ``: всё ещё рано
52. ``: третий цикл ухода: «красный»
53. ``: проверяем: все «красные»
54. ``: теперь можно собирать — список очистится
55. ``: убеждаемся, что плодов на кусте не осталось
